import { z } from "zod";

// Define a schema for the id
export const idSchema = z.string().min(1);

// Tidak ada anotasi tipe, interface, atau type alias pada file ini.
// File sudah valid untuk .js.
// Anda bisa langsung mengganti ekstensi file menjadi .js tanpa perubahan kode.